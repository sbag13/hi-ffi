[package]
edition = "2024"
name = "tests"
version = "0.1.0"

[lib]
crate-type = ["cdylib", "lib", "staticlib"]

[dependencies]
hi-ffi = { path = "../", features = ["cpp"] }
# hi-ffi = { path = "../", features = ["cpp", "swift", "python"] }
serde = { version = "1.0", features = ["derive"] }
