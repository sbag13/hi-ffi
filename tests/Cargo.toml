[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib", "staticlib"]

[dependencies]
hi-ffi = { path = "../", features = ["cpp"] }
serde = { version = "1.0", features = ["derive"] }
