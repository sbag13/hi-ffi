[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib", "lib"]

[dependencies]
hi-ffi = { path = "../", features = ["cpp", "swift"] }
serde = { version = "1.0", features = ["derive"] }
